{
	"name": "monithor-mqtt",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:api": "bun --filter elysia-mqtt dev",
		"dev:web": "bun --filter nextjs-mqtt dev",
		"dev": "bun run --parallel --filter '*' dev"
	}
}