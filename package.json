{
	"name": "elysia-next-mqtt",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:api": "bun --filter api dev",
		"dev:web": "bun --filter web dev",
		"dev": "bun run --filter '*' dev --parallel"
	}
}